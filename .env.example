# API-Football key (https://www.api-football.com) — dev-only, read by scripts/infra,
# never prefixed with VITE_ so Vite never injects it into the public bundle.
API_FOOTBALL_KEY=

# TheSportsDB key — optional, used for team crests. Same hygiene: no VITE_ prefix.
THESPORTSDB_KEY=
